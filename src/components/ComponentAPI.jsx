
// eslint-disable-next-line react/prop-types
const ComponentAPI = ({title,price}) => {
  return (
    <div>
        <h1>{title}</h1>
        <h5>{price}</h5>
    </div>
  )
}

export default ComponentAPI